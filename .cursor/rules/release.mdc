---
description: 发布/发版 cursor-feedback 的流程与硬约束（版本号、CHANGELOG、npm/Open VSX/VS Code Marketplace 三渠道自动发布）。做 release / publish / 发版 / 出包相关任务时务必阅读。
alwaysApply: false
---

# 发版流程

发版用 `standard-version`，发布由 GitHub Actions（`.github/workflows/release.yml`，推送 `v*.*.*` tag 触发）自动完成。**不要手动 npm publish / vsce publish**。

## 发一个版本

```bash
npm run release:patch   # 修 bug；release:minor 加功能；release:major 破坏性改动
git push --follow-tags origin main
```

`standard-version` 会自动：升级 `package.json` 版本号、按 commit 生成 CHANGELOG、打好 `vX.Y.Z` tag。推送 tag 后 Actions 自动发三渠道。

## 三个渠道

| 渠道 | 认证 | 说明 |
| --- | --- | --- |
| npm | OIDC Trusted Publishing | 免 token、永不过期（已在 npmjs 配 trusted publisher，无需 NPM_TOKEN）|
| Open VSX | `OVSX_PAT` secret | Cursor / Windsurf / VSCodium 用户从这里装 |
| VS Code Marketplace | `VSCE_PAT` secret | 缺该 secret 时此步骤自动跳过 |

## 硬约束（踩过的坑，别再犯）

- **禁止给 `package.json` 加 `files` 字段**：它会和 `.vscodeignore` 冲突，导致 `vsce package` 直接报错失败。
  - npm 包瘦身只用 **`.npmignore`**；VS Code 打包只用 **`.vscodeignore`**。两者各管各的、互不影响。
- commit 必须遵循 **conventional commits**（`feat:` / `fix:` / `chore:` …），否则 CHANGELOG 生成不正确。
- `pre-push` hook 会校验 `package.json` 的 version 已写入 `CHANGELOG.md`，没写会被拦下。

## 验证发布是否成功

```bash
npm view cursor-feedback version
curl -s https://open-vsx.org/api/jianger666/cursor-feedback | grep -o '"version":"[^"]*"'
```
